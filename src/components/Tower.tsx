import React from 'react'
import c from './Tower.module.scss'

type TowerProps = { color: 'red' | 'blue'; goal: number; current: number }
const Tower = ({ color, goal, current }: TowerProps) => <div></div>

export default Tower
